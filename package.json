{
  "name": "tactical.mesh.navigator",
  "version": "1.0.0",
  "description": "Автономний тактичний Mesh-навігатор та рація",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp style.css www/ && cp app.js www/"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "@capacitor/android": "^5.7.0"
  }
}
